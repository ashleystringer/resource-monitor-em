<!--<div droppable='true' {{on 'drop' this.dropItem}} {{on 'dragover' this.dragOver}} id="drop-zone"></div>
<div id="test" draggable='true' {{on "dragstart" this.dragStart}}></div>
<div>
    {{#if this.isFile}}
        <button class='button' {{on "click" this.test}}>Submit</button>
    {{/if}}
</div>-->
<div>
    <ImageUpload @url="http://localhost:8080/api/upload" @reset={{this.resetUpload}} @image=""/>
</div>
<div>
    <button {{on "click" this.reset}}>Reset</button>
</div>